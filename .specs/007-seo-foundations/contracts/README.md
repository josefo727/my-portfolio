# Contracts — Index — 007-seo-foundations

Two build-time output contracts (not runtime HTTP boundaries — this remains a fully static site, no server-side third parties, consistent with `001`-`006`'s `contracts/README.md`):

- [`sitemap.md`](./sitemap.md) — `sitemap.xml`, generated by `@nuxtjs/sitemap` (`zeroRuntime`).
- [`robots.md`](./robots.md) — `robots.txt`, hand-authored static file.

Both are consumed by external crawlers (Google, Bing), not by this codebase's own runtime — "provider verification" here means inspecting the real file produced by `nuxi generate`, per `plan.md` §Test strategy's build-level check.
