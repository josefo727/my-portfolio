# Contracts — Index — 004-i18n

No external boundary is touched by this feature. `@nuxtjs/i18n` is a build-time Nuxt module (bundled into the static output, not a runtime service call) — same "no third parties" status as `001`/`002`/`003`'s `contracts/README.md`, confirmed in ADR 0004. Kept as a placeholder per `templates/contracts.md`'s convention.
