# Contracts — Index — 002-visual-design

No external boundary is touched by this feature (pure CSS/layout, no new dependency, no new HTTP/DB/queue integration) — same as `001-nuxt3-portfolio-rewrite`'s `contracts/README.md`. Kept as a placeholder per `templates/contracts.md`'s convention.
