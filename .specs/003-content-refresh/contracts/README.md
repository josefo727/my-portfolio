# Contracts — Index — 003-content-refresh

No external boundary is touched by this feature (adds static data entries and static image assets, no new dependency, no new HTTP/DB/queue integration) — same as `001-nuxt3-portfolio-rewrite`'s and `002-visual-design`'s `contracts/README.md`. Kept as a placeholder per `templates/contracts.md`'s convention.
